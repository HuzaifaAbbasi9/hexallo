import { sectionSpacing } from "@/constants/theme";

const Hero = () => {
  return (
    <section
      className={`${sectionSpacing.Hero} bg-primary-100 font-primary text-accent-100`}
    >
     Hero
    </section>
  );
};

export default Hero;
